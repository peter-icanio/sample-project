name: "python-deploy"
on: push

jobs:
  build:
     runs-on: self-hosted
     
     steps:
     - uses: actions/checkout@v3
     - name: set up python
       run: |
        ls
        pwd
        cd /home/ubuntu/actions-runner/_work/sample-project/sample-project
        ls
        pm2 start "python3 app.py" --name pythoo
       
